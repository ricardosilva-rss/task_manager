<%= simple_form_for([list, task]) do |f| %>
  <%= f.input :title %>
  <%= f.input :description %>
  <%= f.input :card_color, collection: ["yellow", "blue", "green", "pink"] %>
  <%= f.input :location %>
  <%= f.input :date, :include_blank => true %>
  <%= f.submit %>
<% end %>
